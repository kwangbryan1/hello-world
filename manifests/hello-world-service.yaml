apiVersion: v1
kind: Service
metadata:
  name: hello-world-serivce
  namespace: hello-world-ui

spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 80
    nodePort:  30000
  selector:
    app: hello-world
